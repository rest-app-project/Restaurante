<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.restaurante">

    <!-- Permissões para vibração e acesso a internet (WebView) -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_restaurante_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_restaurante_app_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Restaurante">

        <activity
            android:name=".activities.CadastroActivity"
            android:exported="false"
            android:theme="@style/MyTheme"/>

        <activity
            android:name=".activities.LoginActivity"
            android:exported="false"
            android:theme="@style/MyTheme" />

        <activity
            android:name=".activities.MainActivity"
            android:exported="false" />

        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/MyTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>